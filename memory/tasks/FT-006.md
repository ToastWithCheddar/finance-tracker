# TaskSpec: FT-006 — Expand Smoke Checks

- ID: FT-006
- Purpose: Extend `scripts/smoke.sh` to validate one or two contract endpoints and a simple FE preview, keeping it auth-agnostic.

## Context
- Current smoke script checks `/health` and runs a FE build. We’ll add a GET to `/api/info` (no auth) and a basic Vite preview check if feasible.

## Files To Touch
- `scripts/smoke.sh`
- `memory/runbooks/smoke.md` (update with new checks)

## Exact Changes
1) Backend endpoint checks (no auth)
- After `/health`, add a GET to `${BACKEND_URL}/api/info` and assert 200.

2) Frontend preview check (optional)
- If running locally (not in CI): run `npm run preview` in the background and curl the root path to verify 200, then kill the preview. This is optional and can be skipped if ports are busy.

3) Output clarity
- Print clear pass/fail lines for each step.

## Acceptance Criteria
- Script exits non‑zero if `/api/info` fails.
- Works on a clean dev machine without auth configuration.
- Remains fast (<2 minutes).

## Pitfalls
- Avoid requiring auth tokens.
- Ensure preview step is optional and gracefully skipped if it can’t bind a port.

