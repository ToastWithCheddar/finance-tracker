# TaskSpec: FT-010 — Merchants Service Alignment

- ID: FT-010
- Purpose: Align Merchants-related service methods with backend contract and add a tiny adapter if needed for response normalization.

## Context
- Backend routes: `backend/app/routes/merchants.py` (prefix `/api/merchants`), including:
  - `POST /recognize` (query or body includes description)
  - `POST /transactions/{transaction_id}/enrich`
  - `PUT /transactions/{transaction_id}/correct`
  - `GET /suggestions`
  - `GET /stats`
  - `POST /bulk-recognize`
  - `DELETE /cache`
- FE files: `frontend/src/services/merchantService.ts` (or `services/transactionService.ts` methods touching merchants), `frontend/src/hooks/useMerchantEnrichment.ts`.

## Files To Touch
- `frontend/src/services/transactionService.ts` (merchant-related methods)
- (Optional) `frontend/src/api/adapters/merchant.ts` if response shapes need mapping

## Exact Changes
- Verify and align param/body names per OpenAPI for each endpoint above (snake_case on the wire):
  - Ensure `description`, `merchant_name`, and path param `transaction_id` align.
  - For suggestions: params `query`, `limit`.
- If the backend returns well-structured responses already (per generated types), keep as-is; otherwise add a small adapter to standardize DTOs.

## Acceptance Criteria
- All merchant-related calls align with path/param names from the contract.
- Type-check and smoke pass.
- No regressions in merchant recognition/enrichment flows.

## Pitfalls
- Keep changes minimal; avoid reshaping component props.
- Confirm HTTP methods (POST vs PUT) match the contract.

