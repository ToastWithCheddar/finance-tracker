# TaskSpec: FT-009 — Saved Filters Service Contract Sync

- ID: FT-009
- Purpose: Align Saved Filters service with backend contract for list/create/update/delete; ensure param names and shapes match OpenAPI.

## Context
- Backend routes: `backend/app/routes/saved_filters.py` (prefix `/api/saved-filters`).
- FE files: `frontend/src/services/savedFilterService.ts`, `frontend/src/hooks/useSavedFilters.ts`.

## Files To Touch
- `frontend/src/services/savedFilterService.ts`
- (Optional) `frontend/src/api/adapters/savedFilter.ts` if response shapes need mapping beyond trivial renames.

## Exact Changes
- Verify endpoints and align param/body names (snake_case on the wire):
  - List: pagination params (`skip/limit` or `limit/offset` per spec).
  - Create/Update: ensure fields match the contract (e.g., `filters` object, `name`, etc.).
  - Delete: path param id and success shape.
- Map responses to existing FE interfaces; add a tiny adapter only if necessary.

## Acceptance Criteria
- All Saved Filters service calls use contract-aligned paths/params.
- Type-check and smoke pass.
- No UI regressions in saved filters flows.

## Pitfalls
- Keep changes surgical; avoid touching unrelated features.
- Do not change public method signatures unless provably unused.

