# TaskSpec: FT-007 — Fix TypeScript in `frontend/src/utils/account.ts`

- ID: FT-007
- Purpose: Unblock clean smoke runs by fixing pre-existing TypeScript errors in account utilities with surgical changes.

## What To Do
- Open `frontend/src/utils/account.ts` and resolve TypeScript errors by:
  - Adding explicit types where inference fails.
  - Importing existing types from `frontend/src/types/*` where appropriate.
  - Adjusting return shapes to match declared interfaces.
- Avoid broad refactors; keep logic intact.

## Files To Touch
- `frontend/src/utils/account.ts` (and only directly related type imports if needed).

## Acceptance Criteria
- `npm --prefix frontend run type-check` passes.
- `./scripts/smoke.sh` runs clean.

## Pitfalls
- Do not change consumers’ expectations (function signatures/return contracts) unless obviously incorrect and self-contained.
- Prefer adding local types or importing shared ones over introducing new global types.

## Validation Commands
- `npm --prefix frontend run type-check`
- `./scripts/smoke.sh`
