
# Integration TODO

- [ ] Create `.env` file in `frontend` with `VITE_API_URL`.
- [ ] Create `frontend/src/services/authService.ts`.
- [ ] Create `frontend/src/services/accountService.ts`.
- [ ] Update `frontend/src/services/index.ts` and `ServiceRegistry.ts` to include new services.
- [ ] Update `App.tsx` to handle auth state and render a login page or the main app.
- [ ] Update the account list component to use `accountService` to fetch and display real data.
